#include <iostream>
using namespace std;
int main()
{
    int n;
    cin >> n;
    cout << "Enter the size of the array " << endl;

    int arr[n];
    for (int i = 0; i < 8 - 1; i++)
    {
        for (int k = 1; k < 8; k++)
        {
            if (arr[])
        }
    }
}